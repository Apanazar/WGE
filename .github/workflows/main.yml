name: Build and create release

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Type of release (patch/minor/major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  determine-version:
    name: Determine Next Version
    runs-on: ubuntu-latest
    outputs:
      next_tag: ${{ steps.version.outputs.next_tag }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Calculate next semantic version
        id: version
        run: |
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "Latest tag: $LATEST_TAG"
          
          VERSION=${LATEST_TAG#v}
          
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
          
          RELEASE_TYPE=${{ github.event.inputs.release_type }}
          
          case $RELEASE_TYPE in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch|*)
              PATCH=$((PATCH + 1))
              ;;
          esac
          
          NEXT_TAG="v${MAJOR}.${MINOR}.${PATCH}"
          echo "Next tag: $NEXT_TAG"
          echo "next_tag=$NEXT_TAG" >> $GITHUB_OUTPUT

  build:
    name: Build for ${{ matrix.goos }}/${{ matrix.goarch }}
    needs: determine-version
    runs-on: ${{ matrix.runs-on }}
    strategy:
      matrix:
        include:
          - goos: windows
            goarch: amd64
            artifact_name: wge-windows.exe
            runs-on: windows-latest
            ldflags: -H windowsgui -s -w
            cgo_enabled: 1
            
          - goos: linux
            goarch: amd64
            artifact_name: wge-linux
            runs-on: ubuntu-22.04
            ldflags: -s -w
            cgo_enabled: 1
            
          - goos: darwin
            goarch: amd64
            artifact_name: wge-macos-intel
            runs-on: macos-latest
            ldflags: -s -w
            cgo_enabled: 1
            
          - goos: darwin
            goarch: arm64
            artifact_name: wge-macos-silicon
            runs-on: macos-latest
            ldflags: -s -w
            cgo_enabled: 1

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Install Linux dependencies
        if: matrix.goos == 'linux'
        run: |
           sudo apt-get update
           sudo sed -i '/^deb.*noble main/ s/^#*//' /etc/apt/sources.list
           sudo apt-get update
           sudo apt-get install -y \
           libgtk-3-dev \
           libwebkit2gtk-4.0-dev \
           build-essential \
           pkg-config \
           gcc \
           libc6-dev

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo_enabled }}
          VERSION: ${{ needs.determine-version.outputs.next_tag }}

        run: |
          go build -ldflags="${{ matrix.ldflags }} -X main.Version=$VERSION" -o "${{ github.workspace }}/${{ matrix.artifact_name }}" "${{ github.workspace }}/main.go" 

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ github.workspace }}/${{ matrix.artifact_name }}

  create-release:
    name: Create Release
    needs: [determine-version, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.determine-version.outputs.next_tag }}
          release_name: Release ${{ needs.determine-version.outputs.next_tag }}
          body: 
          draft: false
          prerelease: false

      - name: Upload Windows binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/wge-windows.exe/wge-windows.exe
          asset_name: wge-windows.exe
          asset_content_type: application/vnd.microsoft.portable-executable

      - name: Upload Linux binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/wge-linux/wge-linux
          asset_name: wge-linux
          asset_content_type: application/octet-stream

      - name: Upload macOS Intel binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/wge-macos-intel/wge-macos-intel
          asset_name: wge-macos-intel
          asset_content_type: application/octet-stream

      - name: Upload macOS Silicon binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifacts/wge-macos-silicon/wge-macos-silicon
          asset_name: wge-macos-silicon
          asset_content_type: application/octet-stream

      - name: Cleanup
        run: rm -rf ./artifacts/
